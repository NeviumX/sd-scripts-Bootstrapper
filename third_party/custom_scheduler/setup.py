from setuptools import setup, find_packages

setup(name='LoraEasyCustomOptimizer', 
      version='1.0.0', 
      description='The Custom Scheduler that was implemented recently', 
      author='Derrian Distro', 
      packages=find_packages(), 
      zip_safe=False)